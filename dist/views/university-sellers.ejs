<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= university.name %> Sellers - Snap Cart</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #2C3E50;
            --secondary: #fff;
            --accent: #87CEEB;
            --light: #f0f8ff;
            --dark: #2C3E50;
            --gray: #708090;
            --border: #e6f3ff;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: var(--light);
            color: var(--dark);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: var(--secondary);
            padding: 40px 0;
            text-align: center;
            margin-bottom: 40px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            font-family: 'Playfair Display', serif;
            font-size: 36px;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .header p {
            color: var(--gray);
            font-size: 16px;
        }

        .stats {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 20px;
        }

        .stat-card {
            background: var(--light);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            min-width: 150px;
        }

        .stat-number {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary);
            display: block;
        }

        .stat-label {
            font-size: 14px;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-top: 5px;
        }

        .sellers-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
        }

        .seller-card {
            background: var(--secondary);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            border: 1px solid var(--border);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .seller-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }

        .seller-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }

        .seller-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            overflow: hidden;
            flex-shrink: 0;
        }

        .seller-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .default-avatar {
            width: 100%;
            height: 100%;
            background: var(--gray);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
        }

        .seller-info h3 {
            font-size: 20px;
            font-weight: 600;
            color: var(--primary);
            margin-bottom: 5px;
        }

        .seller-meta {
            display: flex;
            gap: 15px;
            font-size: 14px;
            color: var(--gray);
            margin-bottom: 15px;
        }

        .business-description {
            font-size: 14px;
            line-height: 1.5;
            color: var(--gray);
            margin-bottom: 15px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
            line-clamp: 3;
        }

        .seller-stats {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            padding: 15px;
            background: var(--light);
            border-radius: 8px;
        }

        .stat-item {
            text-align: center;
        }

        .stat-value {
            font-size: 18px;
            font-weight: 600;
            color: var(--primary);
            display: block;
        }

        .stat-name {
            font-size: 12px;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .seller-actions {
            display: flex;
            gap: 10px;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s;
            flex: 1;
            justify-content: center;
        }

        .btn-primary {
            background: var(--primary);
            color: var(--secondary);
        }

        .btn-primary:hover {
            background: #333;
        }

        .btn-success {
            background: var(--accent);
            color: white;
        }

        .btn-success:hover {
            background: #1da851;
        }

        .experience-badge {
            display: inline-block;
            padding: 4px 8px;
            background: var(--primary);
            color: white;
            border-radius: 12px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        .empty-state {
            text-align: center;
            padding: 80px 20px;
            background: var(--secondary);
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .empty-state i {
            font-size: 64px;
            color: var(--gray);
            margin-bottom: 20px;
        }

        .empty-state h3 {
            color: var(--dark);
            margin-bottom: 10px;
        }

        .empty-state p {
            color: var(--gray);
            max-width: 400px;
            margin: 0 auto;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
            margin-bottom: 20px;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #333;
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 28px;
            }

            .stats {
                flex-wrap: wrap;
                gap: 15px;
            }

            .stat-card {
                min-width: 120px;
            }

            .sellers-grid {
                grid-template-columns: 1fr;
            }

            .seller-header {
                flex-direction: column;
                text-align: center;
            }

            .seller-actions {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <%- include('./partials/header') %>

    <div class="container">
        <a href="/" class="back-link">
            <i class="fas fa-arrow-left"></i> Back to Home
        </a>

        <div class="header">
            <h1><%= university.name %></h1>
            <p>Approved sellers from <%= university.name %></p>
            <div class="stats">
                <div class="stat-card">
                    <span class="stat-number"><%= sellers.length %></span>
                    <span class="stat-label">Active Sellers</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= sellers.reduce((sum, seller) => sum + seller.productsCount, 0) %></span>
                    <span class="stat-label">Total Products</span>
                </div>
            </div>
        </div>

        <% if (sellers && sellers.length > 0) { %>
            <div class="sellers-grid">
                <% sellers.forEach(seller => { %>
                    <div class="seller-card">
                        <div class="seller-header">
                            <div class="seller-avatar">
                                <% if (seller.profileImage) { %>
                                    <img src="<%= seller.profileImage %>" alt="<%= seller.name %>">
                                <% } else { %>
                                    <div class="default-avatar">
                                        <i class="fas fa-user"></i>
                                    </div>
                                <% } %>
                            </div>
                            <div class="seller-info">
                                <h3><%= seller.name %></h3>
                                <div class="seller-meta">
                                    <span><i class="fas fa-calendar"></i> Joined <%= seller.joinedDate %></span>
                                </div>
                            </div>
                        </div>

                        <div class="experience-badge">
                            <i class="fas fa-chart-line"></i> <%= seller.experience.charAt(0).toUpperCase() + seller.experience.slice(1) %> Seller
                        </div>

                        <% if (seller.businessDescription) { %>
                            <div class="business-description">
                                <%= seller.businessDescription %>
                            </div>
                        <% } %>

                        <div class="seller-stats">
                            <div class="stat-item">
                                <span class="stat-value"><%= seller.productsCount %></span>
                                <span class="stat-name">Products</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><i class="fab fa-whatsapp"></i></span>
                                <span class="stat-name">WhatsApp</span>
                            </div>
                            <div class="stat-item">
                                <span class="stat-value"><i class="fas fa-university"></i></span>
                                <span class="stat-name">Verified</span>
                            </div>
                        </div>

                        <div class="seller-actions">
                            <a href="/seller/<%= seller._id %>" class="btn btn-primary">
                                <i class="fas fa-store"></i> View Shop
                            </a>
                            <a href="https://wa.me/<%= seller.whatsapp.replace(/[^0-9]/g, '') %>?text=Hi%20<%= encodeURIComponent(seller.name) %>%2C%20I%20saw%20your%20shop%20on%20Snap%20Cart!" class="btn btn-success" target="_blank">
                                <i class="fab fa-whatsapp"></i> Contact
                            </a>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="empty-state">
                <i class="fas fa-users"></i>
                <h3>No Sellers Yet</h3>
                <p>No approved sellers from <%= university.name %> yet. Check back later or be the first to apply!</p>
            </div>
        <% } %>
    </div>
</body>
</html>